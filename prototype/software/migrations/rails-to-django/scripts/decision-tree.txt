START: Should I migrate from Rails to Django?
├── Is the migration driven by Python ecosystem needs (ML, data science, AI)?
│   ├── YES → Strong fit — Django integrates natively with NumPy, pandas, scikit-learn [src5, src7]
│   └── NO ↓
├── Is the team already proficient in Python?
│   ├── YES → Migrate incrementally via API boundary (strangler fig) [src3]
│   └── NO ↓
│       ├── Is the team willing to learn Python? (Easier transition than Ruby → JS)
│       │   ├── YES → Invest in training, then migrate [src5]
│       │   └── NO → Stay on Rails or consider Node.js instead
├── Does the app need Django's built-in admin panel?
│   ├── YES → Django admin saves weeks of development for CRUD-heavy apps [src1, src6]
│   └── NO ↓
├── Is the app API-only (no server-rendered HTML)?
│   ├── YES → Use Django REST Framework (DRF) — closest to Rails API mode [src3]
│   │   ├── Need async support?
│   │   │   ├── YES → Django 5.x+ has native async views and ORM [src1]
│   │   │   └── NO → Standard DRF with sync views
│   └── NO → Django templates replace ERB; simpler than you think [src4]
├── Is the app CPU-heavy (ML inference, data processing)?
│   ├── YES → Django is a strong fit — Python excels at computation [src5, src7]
│   └── NO ↓
├── Is the Rails app small and stable with no strategic Python need?
│   ├── YES → Keep Rails — migration cost likely exceeds benefit
│   └── NO ↓
└── DEFAULT → Migrate if Python/Django alignment exists. Otherwise, stay on Rails.
